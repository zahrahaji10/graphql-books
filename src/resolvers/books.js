// define the resolver for books
const books = () => {
  // get the search term

  // make an API Call

  // map through the books API data and transform data

  //  return an array of books object that matches the schema
  return [{}];
};

// get the id

// export module
module.exports = {
  books,
};
